<div class="add-classification login-form">
  <h1>Add New Classification</h1>
  <% if (errors) { %>
    <ul class="notice flash-message">
      <% errors.array().forEach(error => { %>
        <li><%= error.msg %></li>
      <% }) %>
    </ul>
  <% } %>
  <form action="/inv/add-classification" method="POST" class="login">
    <div>
      <label for="classification_name">Classification Name:</label>
      <input type="text" id="classification_name" name="classification_name" required pattern="^[a-zA-Z0-9]+$">
    </div>
    <button type="submit">Add Classification</button>
  </form>
</div>